import does.nt.exist  # pylint: disable=import-error

def toy():
    """toy example"""
    # pylint: disable=undefined-variable
    var_a = 23
    var_b = 42
    var_c = var_a + var_b + var_x
    var_d = var_c - var_y
    # pylint: disable=bad-except-order
    return var_d